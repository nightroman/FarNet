#summary Step/Select/Delete by words in the editor

= Introduction =

This tool alters editor actions on Ctrl-Left/Right, Ctrl-Shift-Left/Right,
Ctrl-Backspace/Delete (step/select/delete by words left or right).

New actions are similar to what many popular editors do on stepping
(Ctrl-Left/Right), selecting (Ctrl-Shift-Left/Right) or deleting
(Ctrl-Backspace/Delete) by words. Example: Visual Studio editor.

Note:
RightControl has nothing to do with the right Ctrl key: "right" means "proper".

Иначе говоря, ходим (Ctrl-Left/Right) и выделяем (Ctrl-Shift-Left/Right) по словам примерно так, так это делает Visual Studio или Word/WordPad (только последние считают символ "подчеркивание" разделителем слов, а студия – нет; RightControl по умолчанию тоже не считает это разделителем, но можно настроить).

----
[http://forum.farmanager.com/viewtopic.php?f=5&t=5622 Обсуждение на форуме]